export const DEFAULT_PLACEHOLDER = 'Search commands…';
export const DEFAULT_EMPTY_MESSAGE = 'No results found.';
export const DEFAULT_ARIA_LABEL = 'Command palette';

/** Matches the CSS transition/animation duration declared in variants.ts (ms). */
export const ANIMATION_DURATION = 150;
